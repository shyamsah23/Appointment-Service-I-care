enum Status {
    SCHEDULED,
    CANCEL,
    COMPLETED
}

type Appointment {
    id: ID,
    patientId: ID,
    doctorId: ID,
    appointmentDate: String,
    status: Status,
    reason: String,
    notes: String
}

type Query {
    getAppointmentById(id: ID!) : Appointment
}